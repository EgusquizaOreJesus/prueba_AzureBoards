'use strict'

// Bucle for
// Estructura de control que se repite varias veces

// quiero mostrar en consola los numeros del 0 al 100
var numero = 100;

for(var i = 0; i <= numero; i++){
    console.log("Vamos por el numero: " + i);
    //debugger; // detiene la ejecucion del codigo
}